---
import { getCollection, type CollectionEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

type Tool = CollectionEntry<'tools'>;

const tools: Tool[] = await getCollection('tools');

// Sort by order
const sorted = tools.sort((a, b) => (a.data.order ?? 999) - (b.data.order ?? 999));
---

<BaseLayout title="Tools | First7">

  <section class="max-w-5xl mx-auto py-16 px-4 space-y-16">

    <!-- HERO -->
    <div class="max-w-2xl">
      <h1 class="text-4xl md:text-5xl font-semibold leading-tight">
        Tools
      </h1>

      <p class="mt-4 text-lg text-[var(--color-muted)]">
        Simple tools to help you live with discipline, purpose, and faith.
      </p>
    </div>

    <!-- FEATURED -->
    {sorted.filter(t => t.data.featured).slice(0,1).map((t) => (
      <a
        href={`/tools/${t.data.slug}`}
        class="block p-8 rounded-2xl border border-[var(--color-border)] bg-[var(--color-primary)]/10 hover:shadow-md transition"
      >

        <p class="text-sm text-[var(--color-muted)]">Featured</p>

        <h2 class="mt-2 text-2xl font-semibold">
          {t.data.title}
        </h2>

        <p class="mt-3 text-sm text-[var(--color-muted)] max-w-md">
          {t.data.description}
        </p>

        <div class="mt-5 text-[var(--color-primary)] font-medium text-sm">
          Open tool →
        </div>

      </a>
    ))}

    <!-- ALL -->
    <div class="space-y-6">

      <h2 class="text-xl font-semibold">
        All Tools
      </h2>

      <div class="grid gap-4 md:grid-cols-2">

        {sorted.map((t) => (
          <a
            href={`/tools/${t.data.slug}`}
            class="p-5 rounded-xl border border-[var(--color-border)] bg-[var(--color-primary)]/5 hover:bg-[var(--color-primary)]/10 hover:shadow-md transition"
          >

            <h3 class="font-medium">
              {t.data.title}
            </h3>

            <p class="mt-2 text-sm text-[var(--color-muted)]">
              {t.data.description}
            </p>

            <p class="mt-2 text-sm text-[var(--color-muted)]">
              Open →
            </p>

          </a>
        ))}

      </div>

    </div>

  </section>

</BaseLayout>